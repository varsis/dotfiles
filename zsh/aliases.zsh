alias reload!='. ~/.zshrc'

alias cls='clear' # Good 'ol Clear Screen command

alias ku="kubectl"

alias test_android="bundle exec fastlane build_test && bundle exec fastlane run_unit_tests && bundle exec danger"
